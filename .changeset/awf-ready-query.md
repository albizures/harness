---
"@albizures/awf": minor
---

Implement `awf ready` with manifest readiness filters, dependency and concurrency gates, deterministic ordering, suggested start command metadata, and `--limit` support.
