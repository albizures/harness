---
"@albizures/awf": minor
---

Require an explicit workflow config or `--config` for CLI execution, document explicit workflow module setup, and keep bundled `agent-development` usage behind explicit imports.
