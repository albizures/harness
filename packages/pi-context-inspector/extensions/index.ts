import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/**
 * Context Inspector scaffold.
 *
 * Planned command: /context-inspector
 * Planned behavior: show a read-only, command-opened context attribution estimate
 * for the current active Pi context.
 */
export default function (_pi: ExtensionAPI) {
  // Implementation intentionally deferred.
}
